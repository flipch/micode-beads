# {{TASK_TITLE}}

> Generated by agent {{TASK_ID}}

## Overview

This document describes the {{MODULE_NAME}} component of the Snake Game project. It covers the public API, configuration options, usage patterns, and integration points with other modules.

## Architecture

The {{MODULE_NAME}} module follows a clean separation of concerns:

- **State management**: Immutable state updates via spread operators
- **Configuration**: Readonly config objects with sensible defaults
- **Event-driven**: Uses an event emitter pattern for cross-module communication
- **Testable**: All public methods are pure functions or have minimal side effects

## Public API

### Types

```typescript
interface {{CLASS_NAME}}Config {
  readonly enabled: boolean;
  readonly tickRate: number;
  readonly gridWidth: number;
  readonly gridHeight: number;
}

interface {{CLASS_NAME}}State {
  readonly initialized: boolean;
  readonly active: boolean;
  readonly timestamp: number;
}
```

### Functions

| Function | Parameters | Returns | Description |
|----------|-----------|---------|-------------|
| `create{{CLASS_NAME}}` | `config?: Partial<Config>` | `{{CLASS_NAME}}` | Factory function for creating instances |
| `initialize` | none | `void` | Initializes the module state |
| `start` | none | `void` | Activates the module |
| `stop` | none | `void` | Deactivates the module |
| `reset` | none | `void` | Resets to initial state |
| `update` | `deltaMs: number` | `void` | Processes a time step |

## Configuration

Default configuration values:

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| `enabled` | `boolean` | `true` | Whether the module is enabled |
| `tickRate` | `number` | `100` | Update interval in milliseconds |
| `gridWidth` | `number` | `20` | Grid width in cells |
| `gridHeight` | `number` | `20` | Grid height in cells |

## Usage

```typescript
import { create{{CLASS_NAME}} } from "./{{MODULE_NAME}}";

const instance = create{{CLASS_NAME}}({
  tickRate: 50,
  gridWidth: 30,
  gridHeight: 30,
});

instance.initialize();
instance.start();

// Game loop
setInterval(() => {
  instance.update(50);
}, 50);
```

## Integration

This module integrates with:

- **Game Loop**: Receives `update()` calls on each tick
- **State Manager**: Reports state changes via events
- **Renderer**: Provides state for rendering via `getState()`
- **Config System**: Reads configuration from the global config cascade

## Error Handling

All errors are handled gracefully:

- Invalid configuration values are ignored (defaults used)
- Operations on uninitialized modules trigger auto-initialization
- State mutations are atomic (no partial updates)

## Testing

Run tests with:

```bash
bun test tests/{{MODULE_NAME}}.test.ts
```

See the test file for comprehensive coverage of initialization, state transitions, edge cases, and error handling.
