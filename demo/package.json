{
  "name": "micode-beads-demo",
  "version": "1.0.0",
  "private": true,
  "description": "100-agent stress test: builds a snake game in parallel",
  "scripts": {
    "start": "bun run orchestrator.ts"
  }
}
